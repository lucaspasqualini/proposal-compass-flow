

# Aumentar o card de detalhes do projeto

## Mudança
Alterar a classe do `DialogContent` de `max-w-3xl` para `max-w-5xl` e ajustar o `max-h-[90vh]` para `max-h-[95vh]`, dando mais espaço horizontal e vertical ao card.

## Arquivo alterado
- `src/components/ProjectDetailDialog.tsx` — linha 83: trocar `max-w-3xl max-h-[90vh]` por `max-w-5xl max-h-[95vh]`

